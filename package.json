{
  "name": "babel-plugin-transform-react-require",
  "version": "1.0.0",
  "description": "Make files using JSX require React (or other implementations)",
  "repository": "https://github.com/mikaelbr/babel-plugin-transform-react-require",
  "license": "MIT",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register test/ --timeout 10000",
    "prepublish": "npm run build",
    "build": "mkdir -p dist; babel src/index.js --out-file dist/index.js"
  },
  "main": "dist/index.js",
  "keywords": [
    "babel-plugin"
  ],
  "dependencies": {
    "babel-template": "^6.1.18"
  },
  "devDependencies": {
    "babel-cli": "^6.1.18",
    "babel-core": "^6.1.21",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.1.20",
    "babel-plugin-transform-react-jsx": "^6.1.18",
    "babel-preset-es2015": "^6.1.18",
    "mocha": "^2.3.3"
  }
}
